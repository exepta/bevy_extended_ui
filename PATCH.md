## Patch Notes

### Version 1.5.0

- NEW: added `<a>` tags support
- NEW: added `<slider type="range">` support
- NEW: added `<input type="file">` support
- NEW: added `<badge>` support
- CHANGED: `img` now supports `preview` attribute
